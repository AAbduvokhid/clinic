# Server
server.port=8081

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/clinic?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=lingdon0072
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable data.sql ? data already exists in MySQL
spring.sql.init.mode=never

# Disable open-in-view warning
spring.jpa.open-in-view=false

# JWT
jwt.secret=your-super-secret-key-must-be-at-least-32-characters-long
jwt.expiration=86400000
#logging.level.org.springframework.security=DEBUG

#spring.liquibase.change-log=classpath:changelog/db.changelog-master.xml
#spring.liquibase.url=jdbc:mysql://localhost:3306/clinic?serverTimeZone=UTC
#spring.liquibase.user=clinic
#spring.liquibase.password=lingdon
#spring.liquibase.enabled=false
#clinic-secret=clinic-secret